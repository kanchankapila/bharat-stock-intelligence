fastapi==0.136.1
uvicorn[standard]==0.47.0
pydantic==2.13.4
python-dotenv==1.2.2
requests==2.34.2
httpx==0.28.1
# tenacity: retry/backoff decorators used by ingress_governor.py
tenacity==9.1.4
beautifulsoup4==4.14.3
pandas==2.3.3
# polars: ONE real consumer -- densify_feature_matrix.py, which relies on polars' null
# semantics (NaN is not null; forward_fill propagates over nulls) for the feature-matrix
# densification. Do NOT read the low import count as "unused and removable": the 2026-09-03
# bulk pass that put `import polars as pl` at the top of ~200 src/server/*.py files was
# REVERTED (the injected to_polars_df helper had zero call sites), so one importer is the
# correct post-revert state, not a leftover.
# CI installs THIS file, not the repo-root requirements.txt -- an import-time dep that
# only reaches the root file is an import error on every clean checkout.
polars==1.44.1
# DO NOT add pyarrow here. It was added 2026-09-03 to make to_polars_df()'s pl.from_pandas()
# path work (AF-20260829-21) and reverted the same day: on this Windows box `import pyarrow`
# BEFORE `import torch` breaks torch outright -- `OSError: [WinError 1114] ... c10.dll`.
# Reproduced deterministically: torch-then-pyarrow OK, pyarrow-then-torch fails, and the full
# pytest suite went red at collection (dl_engine/nlp_engine/scoring_engine). The helper it was
# added for (to_polars_df) had ZERO call sites and has since been deleted, so pyarrow bought
# nothing and cost the whole torch-dependent ML stack. The rule still stands for any FUTURE
# need: build the frame dtype-aware from numpy/.tolist() instead of pl.from_pandas()
# -- see AF-20260829-21's own recorded alternative.
numpy==2.4.6
scipy==1.17.1
scikit-learn==1.8.0
ta==0.11.0
SQLAlchemy==2.0.49
psycopg2-binary==2.9.12
yfinance==1.3.0
joblib==1.5.3
tqdm==4.67.3
catboost==1.2.10
lightgbm==4.6.0
xgboost==3.2.0
optuna==4.9.0
# Install CUDA build (CPU build is default on PyPI):
# python -m pip install "torch==2.9.1+cu128" torchvision torchaudio --index-url https://download.pytorch.org/whl/cu128
torch>=2.9.1
transformers==5.10.1
tradingview-ta==3.3.0
tradingview-screener==3.2.0
curl_cffi==0.15.0
nse==3.2.1
matplotlib==3.10.9
hmmlearn==0.3.3
langchain==1.3.9
langgraph==1.2.5
langchain-community==0.4.2
langchain-google-genai==4.2.5
chromadb==1.5.9
sentence-transformers==5.5.1
duckduckgo-search==8.1.1
pytest==9.1.0
pytest-asyncio==1.4.0
tabulate==0.10.0
