# Audit findings ledger

Carried across `/weekend-audit` runs by the `audit-loop` skill. **Never delete a row** — close it
with a date. A finding that reappears after being closed is a much stronger signal than a fresh
one, and only this file can show that. An open row surviving 3 runs is itself a finding.

Lanes: **FIX** (mechanically safe) · **EVIDENCE** (touches a score/weight/threshold — needs a
measurement first) · **INVESTIGATE** (symptom, unproven cause) · **ACCEPT** (known, bounded).

| ID | Found | Class | Finding | Lane | Status | Immunized | Closed |
|---|---|---|---|---|---|---|---|
| AF-20260815-01 | 2026-08-15 | temporal | `scripts/integrity_sweep.py` anchors on raw `MAX(date)` (`latest_date_expr`, L49-71). Enrichment columns are written by `ml-daily-ops` in the evening against the *previous* session, so the newest date is always the one enrichment hasn't reached — the sweep reports ~25 false DEAD columns every run. `087f399` fixed this exact bug in `dataQualityChecks.ts` and its own message says it was reintroduced in **both** files, but only one was fixed. | FIX | **fixed** 2026-08-16 — `latest_date_expr()` now anchors on the newest date strictly older than the last successful `ml-daily-ops` (`job_heartbeat`), mirroring `ml-signal-columns-populated`, with a second-newest fallback when no heartbeat exists so it can never silently revert to the same-day read | anchor + rationale documented in the function's own docstring, naming `087f399` and this ID | 2026-08-16 |
| AF-20260815-02 | 2026-08-15 | deploy | `bharat-server` booted 2026-08-15 05:54 UTC; **11 commits touching `.ts`/`.py` have landed since**, latest 15:43 IST. `.ts` is not hot-reloaded — the running server is ~4h20m behind `HEAD`. | FIX | **fixed** 2026-08-16 — all 4 pm2 processes restarted 11:15 UTC, which is *after* `HEAD` (`e7d7700`, 07:03 UTC). Note the working tree is not deployed, but that is uncommitted work, not drift | — | 2026-08-16 |
| AF-20260815-03 | 2026-08-15 | schema | `data_quality_history` exists live but is absent from `db/schema.postgres.sql` — created via self-creating DDL in `dataQualityChecks.ts:1899`, never reflected back. `npm run schema:drift` fails on it. | FIX | **fixed** 2026-08-16 — verified live: `schema:drift` → "Schema clean", 212 tables parsed = 212 live. Now load-bearing beyond tidiness: vitest builds its throwaway schema from this file | — | 2026-08-16 |
| AF-20260815-04 | 2026-08-15 | test | `signalOutcomesServiceSource.test.ts > excludes confluence-sourced outcomes` fails (`expected +0 to be 1`). Traced: passes 4/4 at `HEAD` in a clean worktree, fails in the working tree. Cause is the in-flight `AND label_definition = 'path_barrier'` filter in `getWinRateStats`; the fixture inserts a row without that column. | FIX | **superseded** 2026-08-16 — that session's work landed; the file is one of 27 converted to the Postgres substrate. The `expected +0 to be 1` signature turned out to be the general write-through-SQLite/read-through-Postgres class, now in `recurring-bugs.md`. Re-verify under `vitest run`, not as this row | — | 2026-08-16 |
| AF-20260816-09 | 2026-08-16 | deploy | `envConfig.ts:23-35` still validates `USE_POSTGRES` at bootstrap and pushes to `FATAL` (hard exit) on any value that isn't `"true"`/`"false"`/unset, with the reason *"Any other value silently routes the app onto SQLite."* **That reason has been false since 2026-08-15** — `usePostgres()` reads no env var, so the variable routes nothing. Net effect: a stale `USE_POSTGRES=1` in someone's `.env` now hard-crashes `bharat-server` at boot over a setting that does nothing, and explains the crash with a fallback that no longer exists. Found by a docs-staleness sweep, not by a failure. | FIX | **fixed** 2026-08-16 — spelling check deleted outright. The connection-info check was **kept and widened**: it no longer hangs off `USE_POSTGRES=true` being present, because with Postgres unconditional a missing `POSTGRES_URL`/`POSTGRES_HOST` is always a real misconfiguration. Now calls the existing `isPostgresConfigured()` rather than re-reading env. **Also caught while fixing:** the boot log line read `${usePg === 'true' ? 'PostgreSQL/TimescaleDB' : 'SQLite'}`, so a correctly-configured server printed **"DB engine: SQLite"** at startup — the worst possible thing to log while debugging a database problem | header comment states why the check was removed, so it is not restored | 2026-08-16 |
| AF-20260816-10 | 2026-08-16 | tooling | Two scripts still gate themselves on the dialect and print a SQLite warning that can never be true: `scripts/run_data_quality_checks.ts:19-22` and `scripts/ci_smoke_test.ts:32-35` both do `if (!usePostgres()) { console.error("...refusing to run against SQLite"); process.exit(1) }`. `usePostgres()` returns `true` unconditionally, so **the guard can never fire** — dead code whose message describes a fallback that no longer exists. Inverse of the "monitor that fires 16/16" class already in `recurring-bugs.md`: a check that can never fail carries exactly as little information as one that always fires. Harmless today (nothing breaks), but it is what made `docs/FETCHER_HEALTH_TRACKER.md` claim `npm run dq:check` "needs `USE_POSTGRES=true`" — a prerequisite that has not existed since 2026-08-15. | FIX | **fixed** 2026-08-16 — both guards deleted; the `pgHealthy()` retry loop underneath is untouched and remains the real gate. Both scripts' header comments also claimed `requires USE_POSTGRES=true`, which is what propagated the false prerequisite into `FETCHER_HEALTH_TRACKER.md` — corrected too | in-place comments record why the guard was removed | 2026-08-16 |
| AF-20260815-05 | 2026-08-15 | data | `mf_sector_allocation` empty; `insider_transactions` 75.3d stale; `regime-edge-trust-floor` at 1 of 2 regimes. All three pre-existing DQ warns, none critical. | INVESTIGATE | **open** (run 3) — `mf_sector_allocation` is recorded in memory as a dead upstream AMFI endpoint; insider filings are sparse by nature (warn-only by design). Now surviving its 3rd run: per this file's header that is itself a finding — decide ACCEPT-with-rationale or fix, don't roll it again | — | — |
| AF-20260816-11 | 2026-08-16 | temporal | **Third occurrence of the enrichment-lag anchor bug**, in the same file as one of its two prior fixes. `technical-signals-feature-coverage` (`dataQualityChecks.ts:1828`) still anchors on `MAX(date) WHERE date < CURRENT_DATE` → **2026-08-14**, while its sibling `ml-signal-columns-populated` (:1674) and `integrity_sweep.py` (AF-20260815-01, fixed yesterday) both anchor on the day before the last successful `ml-daily-ops` → **2026-08-13**. Enrichment columns are written the evening *after* the scan, so the newest date always reads them as 100% NULL. **Reclassified FIX → EVIDENCE during remediation, because the mechanical fix was tested and is wrong.** Swapping in the heartbeat anchor makes it *worse*, not better. Dead-column count per date, measured live with the check's own `jsonb_each` logic: **08-10: 59 · 08-11: 60 · 08-12: 45 · 08-13: 46 · 08-14: 15**. The old anchor picks 08-14 (**15** dead), the heartbeat anchor picks 08-13 (**46**). The reason is that *different writers land on different dates*: `delivery_pct`/`iv_hv_ratio`/`days_to_next_results`/`sector_global_corr_21d` are all **0 on 08-14** and populated on 08-13 (1939/2185/1820/1402), while the whole `mc_*` block is the reverse. **There is no single enrichment-complete date, so no anchor choice is correct.** The real defect is larger than the anchor: the check grades a **moving anchor against a fixed baseline of 53** with bands warn>55 / fail>65, while the quantity's own day-to-day range is **15–60**. Natural variance exceeds the entire warn/fail band, so the verdict is decided by which weekday it runs, not by whether a writer regressed — `recurring-bugs.md`'s threshold-below-noise-floor class. Needs per-column/per-writer expected-cadence, not a scalar count. **Deliberately not fixed**: changing the anchor would trade one set of false positives for another and look like progress. | EVIDENCE | **open** | — | — |
| AF-20260816-12 | 2026-08-16 | data | **Trendlyne blocked upstream, 12 days, and the recorded root cause is wrong.** `trendlyne-midweek` ran today 11:48 UTC and failed; 26/33 runs failed; **no success since 2026-08-04**. Error is `405 Client Error: Not Allowed` on *every* id, for two fetchers: `adv-technical-analysis/{tlid}/24/` and `share-price/price-performance-analysis/{tlid}/`. Memory (`job_scheduling_and_reliability`) attributes this job's failures to "catch-up retries with no resumability", marked "fixed + live-verified" 2026-08-15 — that fix was real but addressed wall-clock waste, **not this**. The endpoints are 405-blocked. Per `feedback_failing_urls.md`: reported, replacement deliberately not researched. Note the fetcher's own early-abort ("aborting early after 20 consecutive failures") worked correctly. | INVESTIGATE | **update 2026-08-17** — `trendlyne-midweek` had its **first success since 2026-08-04** at 03:56 UTC today (`last_status='success'`, `fail_count` still 30/38 historically — this is one good run, not a reversal). Correlates with `bf3073c` (null-section-key fix) landing 2026-08-16, not with today's WAF-bounding commit (`f498061`, 08:47 IST, which is *after* the 03:56 UTC success). `trendlyne_price_analysis` is also actively climbing (write at 07:18 UTC today) per today's rotation-sort fix (`9ce7138`). **Still open** — one success does not establish the 405 is resolved upstream; re-check next run — **update 2026-08-18**: `job_heartbeat` unchanged since (`last_run_at = last_success_at = 2026-08-17T03:56:28Z`, still 38 runs/30 fails). Checked the cron (`trendlyneWeekly.jobs.ts:175`, `30 14 * * 2` — Tuesday 14:30 UTC/20:00 IST) against today's date (Tue 08-18): the 03:56 UTC run that succeeded was NOT this week's regular slot (which is still hours away as of this check), so there's genuinely no new data yet, not a stall. Re-check after today's 14:30 UTC run | — | — |
| AF-20260816-13 | 2026-08-16 | monitoring | **Both 405-blocked tables PASS their freshness checks.** `trendlyne-price-analysis-freshness` and `trendlyne-adv-tech-daily-freshness` read `max(date)` only, and a partial write keeps that recent. Live coverage against 2,234 mapped tlids: `trendlyne_price_analysis` **165 symbols (7.4%)** on its newest date and **357 rows in its entire lifetime**; `trendlyne_adv_tech_daily` **1,350 (60%)** on 08-14 but 139/125/125 on the days before. Textbook `recurring-bugs.md` "a fresh table is not a delivered feature" — the freshness mandate in `data-sources.md` is satisfied and still blind. Needs a coverage-ratio check, not a freshness one. | FIX | **fixed + live-verified** 2026-08-16 — new hand-rolled check `trendlyne-per-symbol-fetcher-coverage` counts DISTINCT symbols on each table's own newest date against the mapped-tlid universe. Live: **FAIL** at `trendlyne_price_analysis 165/2234 (7.4%)`, while both freshness checks still report PASS in the same run — that contrast is the negative control | **rung 3** (data-quality check). Verified it **discriminates** rather than blanket-firing: `adv_tech_daily` 60.4% → pass and `price_analysis` 7.4% → fail in the same run, the discrimination test `threshold-calibration-audit` demands | 2026-08-16 |
| AF-20260816-14 | 2026-08-16 | deploy | **AF-20260815-02 recurred within 24h of being closed.** `bharat-server` up since 11:15 UTC; `HEAD` `c7099a3` committed **12:55 UTC** and touches `server.ts`, `src/server/envConfig.ts`, `src/server/insightService.ts`. `.ts` is not hot-reloaded, so the running server does not contain the AF-09/AF-10 fixes that same commit landed. The closed row is not wrong — the gap simply reopens on every commit. A one-shot manual restart cannot hold this closed. | FIX | **open, by construction — re-checked clean today, 2026-08-18.** `bharat-server` uptime_since `2026-08-18T10:40:46Z` (restarted mid-session to deploy AF-47's `technicalSignalsService.ts` fix); every `.ts`-touching commit since (`c9267a0`, 10:42:20Z) landed AFTER that restart in wall-clock terms but the file edits were on disk BEFORE the restart (edit→restart→commit ordering, confirmed via `git log --format=%cI`), so the server is current. The only commit after that (`5d4f973`) is `online_learner.py`, a `.py` file invoked via `runPython()` subprocess — no restart needed, Python has no hot-reload gap the way `.ts` does. **This row stays open by design, not as a bug** — it is a recurring check, not a one-time fix; the ledger's job is to keep re-verifying it each pass, which this entry now records | — | — |
| AF-20260816-15 | 2026-08-16 | stability | `bharat-server` `restart_time=38` (was 3 on 2026-08-15). Error log carries a **native libuv abort**, twice at 11:51 UTC: `Assertion failed: !(handle->flags & UV_HANDLE_CLOSING), file src\win\async.c, line 94`. That is a process-level crash, not a caught exception, so nothing in the app's own error handling sees it. No alert fired. | INVESTIGATE | **open** | — | — |
| AF-20260816-16 | 2026-08-16 | docs | ~~`SQLITE_DECOMMISSION_PLAN.md` understates the remaining work 2.8×~~ — **RETRACTED same session, the finding was mine and it was wrong.** I read the headline **37** against a static `grep -rl sqlite3.connect` of **102** and called it staleness. They are two different metrics and both are correct: `conftest.py:394` `pytest_terminal_summary` counts `_SHIM_USERS`, the files that **actually invoke the shim at runtime**, and it printed **37 on the full 2,025-test suite** — not on a subset. The other ~65 files contain the string inside `test_live_datasource_*.py` bodies that are **skipped by default** (230 skipped; they need `RUN_LIVE_DATASOURCE_TESTS=1`), so they never reach the shim in CI. The doc's inventory row (L177, ~101) is the static count and its headline (37) is the runtime count. **The one real residue is wording, not arithmetic:** `CLAUDE.md:112` says "37 files still **call** `sqlite3.connect`" — 102 call it; 37 route through the shim on a default run. | FIX | **open (downgraded to a wording fix)** — the 2.8× claim is withdrawn | — | — |
| AF-20260816-17 | 2026-08-16 | monitoring | `win-probability-scored-in-time` renders a verdict off **n=1**. `technical_signals.win_probability_scored_at` is stamped on **1 of 10,947** rows across 08-10…08-14, and `measurement.md` explicitly records that single row as *"an artifact of a manual test write, NOT the real cadence"*. The check has no minimum-sample guard: `scored === 0` → pass, `scored === 1` → full verdict. It has warned on **36/36 runs**. Separately its comment (:1725-1731) and its own remediation text ("the weekly scorer has not run… re-check after the next `ml-ensemble-train` run") restate the provenance claim **`measurement.md` withdrew on 2026-08-15** — scoring runs *daily* via `queues.ts:1037` `pythonApi.scorePending()`, not weekly. Note the empty column itself is **not** a bug: migration `1787050000000` landed 08-15 and no trading day has elapsed since (08-15 Sat, 08-16 Sun). First real data is Monday 08-17. | FIX | **fixed + live-verified** 2026-08-16 — added a `MIN_SAMPLE = 100` floor: below it the check declines to judge instead of averaging a partial write. Live verdict moved **WARN → PASS** with detail *"Only 1 row(s)… below the 100-row floor… too thin to judge"*. The withdrawn weekly-cadence claim in the check's comment and in its own remediation text was corrected to name `queues.ts:1037` `pythonApi.scorePending()` | **rung 4** — 2 negative-controlled tests in `dataQualityChecks.test.ts`; with the guard stubbed to `if (false)` the new test fails `expected 'warn' to be 'pass'`, restored green. A third test pins that a full 2,192-row batch at 5.2d still FAILS, so the floor cannot swallow a real defect | 2026-08-16 |
| AF-20260816-18 | 2026-08-16 | tooling | `scripts/integrity_sweep.py` full-repo run produced **0 bytes of output in 40 minutes** and had to be abandoned. Not hung on the DB — it holds one `idle in transaction` connection with `wait_event=ClientRead`, i.e. the server is waiting on the client. stdout is block-buffered when piped, so nothing flushes until exit. Scoped (`--table technical_signals`, `python -u`) it returns in seconds. As written it cannot be used inside an audit window, which is the one place it matters. | FIX | **fixed + verified** 2026-08-16 — every finding now streams as it is discovered (`[i/total] [TAG] table: N dead, M frozen`, `flush=True`) instead of every `print` sitting after the loop. Verified: the progress line appears immediately on a scoped run, and the sorted summary still prints in full | **rung 5+** — the WHY is in an in-place comment naming `recurring-bugs.md`'s "a step at the end of a script that routinely gets killed never runs at all", of which this was the reporting-form instance | 2026-08-16 |
| AF-20260816-19 | 2026-08-16 | data | `screener_catalog.source` still holds **both cases** live: `trendlyne`/`Trendlyne` (987/343), `etnow`/`ETnow` (447/425), `moneycontrol`/`MoneyControl` (133/109) — 877 mixed-case rows of 2,539. The 2026-08-13 fix harmonized `signal_bias` (**verified: 0 screener_names with >1 bias**) and both readers `recurring-bugs.md` named (`intraday_ranker.py`, `movement_predictor.py`) **now correctly use `LOWER()`** — so this is latent, not active. Residual: any *future* reader written with exact case silently sees **25.8% fewer** Trendlyne rows (987 vs 1,330). | ACCEPT | **open (latent)** — a static check for exact-case `source =` on this table is the cheap immunization | — | — |
| AF-20260816-20 | 2026-08-16 | job | `marketsmojo_financials_fetcher` killed at its full **40-minute** budget today (`queues.ts:1235`). It degrades gracefully rather than starving — **hypothesis of alphabetical truncation was tested and disproved**: coverage spans A:149…Z:14 across 1,680 symbols, and the `key in known` incremental guard (the 2026-08-14 fix) is correctly in place at :141. But only **431 of 1,680** symbols refresh per 2 days → a full cycle takes ~8 days, and `marketsmojo_financials_history` is the only table over the bloat threshold (4.17M live / 426k dead, 9.3%, 42 UPDATEs per INSERT). The fetch happens *before* the known-values lookup, so an already-current symbol still costs a full HTTP round-trip. | FIX | **fixed + live-verified 2026-08-18.** Built the missing signal the earlier diagnosis said was needed: `marketsmojo_financials_checked (symbol PRIMARY KEY, checked_at)` (migration `1787090000000`), a per-symbol "we already asked" marker independent of `marketsmojo_financials_history.fetched_at` (which only advances on a genuine value change, so it can't answer "did we check recently"). `run()` now skips a symbol found in `load_recently_checked()` (7-day window, matching the job's own weekly cadence per `queues.ts`'s comment) without any HTTP call — but only on the default full-universe sweep; `--full` and an explicit `--symbols` list both bypass the filter, since both mean "check these now regardless of recency." Discovered while reading the job's real registration (`processMlWeeklyRetrain`, `queues.ts:1235`) that this job is **weekly, not the ~2-day cadence the original finding assumed** — doesn't change the fix, but means the staleness win compounds across a longer window than first thought (without the fix, every weekly run restarts from the same point in the sorted symbol list and re-pays the round-trip for everything it already covered days earlier). **Live end-to-end verified**: fetched RELIANCE explicitly (`--symbols RELIANCE`, bypasses the filter as designed) → `mark_checked` wrote a live row (`checked_at=2026-08-18T11:16:19Z`, confirmed by direct query); immediately re-ran the default full-universe path with `load_sid_map` scoped to just RELIANCE → **0 HTTP calls, 1/1 symbols correctly skipped** ("checked within 7d"). Applied via `npm run migrate:up` against real `POSTGRES_URL`, `schema:regen` + `schema:drift` clean (213=213) | **rung 4** — 4 new tests in `test_marketsmojo_financials_staleness_skip.py` (never-checked not excluded, recently-checked excluded, idempotent upsert, old row falls outside the window), negative-controlled: stashing the fetcher's fix makes the whole file fail to import (`ImportError: cannot import name 'load_recently_checked'`), confirmed, then restored | 2026-08-18 |
| AF-20260815-06 | 2026-08-15 | docs | `CLAUDE.md:82` claimed `dataQualityChecks.ts` holds 62 checks. Live count is 147 (22 critical); source has ~155 `id:` entries. Understated 2.4×. | FIX | **fixed** 2026-08-15 → `~150` | — | 2026-08-15 |
| AF-20260815-07 | 2026-08-15 | tooling | `/weekend-audit`'s own service probe used `/` for the three FastAPI services, which have no root route — three false DOWNs every run. Found by running it. | FIX | **fixed** 2026-08-15 → probe `/docs` | note in skill Lane 2 | 2026-08-15 |
| AF-20260815-08 | 2026-08-15 | tooling | `/weekend-audit` and `/threshold-calibration-audit` asserted `data_quality_results` has no history. `data_quality_history` (append-only) landed the same day, 265 rows / 152 checks. The verdict-variance query was pointed at the wrong table. | FIX | **fixed** 2026-08-15 → both skills repointed | — | 2026-08-15 |
| AF-20260817-21 | 2026-08-17 | frontend | **`Alpha vs Nifty 50` displayed a headline number 100× too large on two of six dashboard shells.** `V2Dashboard.tsx:105` and `V3Dashboard.tsx:1207` both rendered `(dashboardData.overall.alpha_vs_nifty * 100).toFixed(2)`, but `performance_tracker.py`'s `alpha_vs_nifty()` (:207-228) already returns a **percentage-point value** (`signal_ret - nifty_cum`, both terms already ×100 inside the function) — unlike `win_rate` in the same row, which genuinely is a 0-1 fraction and correctly needs `*100`. Live value at time of finding: `strategy_performance.alpha_vs_nifty = 14.923` (real DB row, `segment='overall'`, `horizon_days=15`), rendered on-screen as **"+1492.30% EXCESS OUTPERFORMANCE"** — found via a Lane 5 browser screenshot of the v2 shell, not a code-only review. A number that implausible on a live dashboard is exactly the "looks healthy, is actually broken" shape `recurring-bugs.md` exists to catch. Separate, NOT fixed: whether `strategy_performance`'s underlying `win_rate`/`sharpe_ratio` methodology (no cost/turnover accounting, no per-date panel construction) is itself trustworthy to show a user at all, given `measurement.md`'s precedent that the platform's own precomputed `screener_reliability`/`screener_performance_v2` "did not survive contact with a from-scratch measurement." That question is out of scope for a display-arithmetic fix and needs `/measurement-integrity-review`. | FIX | **fixed + live-verified** 2026-08-17 — removed the stray `* 100` in both files (pure display arithmetic, no backend score/weight/threshold touched, so no backtest evidence required per `verify-gate.mjs`'s rule). `tsc --noEmit` clean. Re-screenshotted the live v2 shell after the edit: now reads **"+14.92% EXCESS OUTPERFORMANCE"**, matching the real DB value exactly | — | 2026-08-17 |
| AF-20260817-22 | 2026-08-17 | temporal | **Two more instances of the write-anchor bug, same job as the already-documented pattern.** `mc_index_oi_fetcher.py:313` and `nt_oi_snapshot_fetcher.py:202` both used raw `date.today()`/`_date.today()` as the `date` column in an upsert into `index_max_pain` (composite PK `(source, index_name, date, expiry)` — the exact table this repo's own `data-sources.md` documents as having had a provider-collision PK bug already). Both fetchers run inside `processMlDailyOps`, the same job independently documented (`block_deal_fetcher.py`'s own comment, `as_of.py`'s docstring) as regularly finishing after midnight IST — a post-midnight run mislabels the just-closed session as the next calendar day, silently. Same class as 11 already-fixed instances in `recurring-bugs.md`'s "Dates & scheduling" table; these two were simply never converted. Found by `/temporal-correctness-audit` (Lane 6, rotation group 4). | FIX | **fixed + verified** 2026-08-17 — both swapped to `as_of.logical_trading_date()`, matching the sibling fetchers in the same job (`block_deal_fetcher.py` et al.) exactly. Dropped `nt_oi_snapshot_fetcher.py`'s now-unused `from datetime import date as _date` import. `ast.parse` clean on both files; `test_mc_index_oi_max_pain.py` + `test_nt_oi_snapshot_index_max_pain.py` (5 tests) pass; full pytest + `check_recurring_bugs.py` re-run clean | matches the existing pattern the 11 prior fixes established — no new immunization needed, this is the same signature `check_recurring_bugs.py` already recognizes when changed | 2026-08-17 |
| AF-20260817-23 | 2026-08-17 | monitoring | **`dq-uninformative-checks` only watches for stuck-BAD verdicts, never stuck-GOOD.** Its SQL filters `distinct_status = 1 AND only_status <> 'pass'` — a check that is structurally incapable of ever firing non-pass (too loose a threshold, an unreachable condition) is invisible to this meta-check no matter how many runs accumulate, same blind-spot shape as the "monitor that fires 16/16" class in `recurring-bugs.md`, inverted. The check's own pre-existing comment already described "permanently-green reported separately from permanently-red" as the intent — it was documented, never wired up. | FIX | **fixed + live-verified** 2026-08-17 — added a `stuck_good` column to the same query, gated at a much higher bar (200+ runs, ~4+ days at this check's ~30-min cadence) than `stuck_bad`'s 10-run bar, specifically so a check merely healthy for a day or two isn't reported the same as one structurally incapable of failing. `data_quality_history` is only ~2-3 days old, so live output correctly shows 0 stuck-good today — the mechanism now exists and will fire once real history accumulates, without generating today's false noise. 4 new tests, one of which negative-controls both branches together | **rung 4** — negative-controlled: 3 of 4 new tests fail with the stuck_good column removed (`test 'reports stuck-bad and stuck-good together'`, `'passes when nothing is stuck'`, and the always-PASS-flagged test), restored green | 2026-08-17 |
| AF-20260817-24 | 2026-08-17 | monitoring | **Two undocumented threshold constants gate live demotions in the canonical ranker with no measured-null derivation.** `unified_ranker.py:502` `CORRELATION_CLUSTER_THRESHOLD = 0.65` and `unified_ranker.py:981` `FACTOR_CROWDING_THRESHOLD = 0.70` both had comments explaining the *mechanism* but not why that specific cut point was chosen. Separately, `backtest_optimizer.py`'s promotion gate (`UPDATE_THRESHOLD = 1.05`) had no run-to-run noise check, the same class `recurring-bugs.md`'s HMM-regime-retrain entry already burned once. Found by `/threshold-calibration-audit` (Lane 6, rotation group 4). | EVIDENCE | **measurement session run 2026-08-17, split three ways — see AF-27/28/29 for the real numbers.** Not fixed by picking new values, which would repeat the exact "fabricated backtest" failure mode this row originally warned against; fixed by actually measuring, which is the discipline `verify-gate.mjs` asks for | — | — |
| AF-20260817-27 | 2026-08-17 | measurement | **`CORRELATION_CLUSTER_THRESHOLD=0.65` — measured, and it's real.** Calling the production `unified_ranker.cluster_by_correlation()` directly (not reimplemented) against 5.5 years of `stock_ohlcv` (2021-01-01→2026-08-17, 300-symbol liquid universe fixed at measurement start to avoid look-ahead universe selection): pairs whose 60-day trailing correlation cleared 0.65 on date D showed materially higher **forward** (D→D+60) correlation than "near-miss" pairs sitting at 0.50–0.65 — mean forward correlation 0.545 vs 0.376, diff +0.169, **t=16.47, p<0.0001, 21/21 sampled dates agreeing** (quarterly snapshots, per-date-then-averaged per `measurement.md`'s panel spec). The mechanism the risk cap rests on — "measured correlation predicts continued co-movement" — is genuinely true in this data, not an assumption. **Not established**: whether 0.65 specifically beats a nearby cut (0.60/0.70) — the result shows the relationship is monotonic and strong, not that 0.65 is the precise optimum. | EVIDENCE | **not changed, and doesn't need to be** — the measurement supports the existing value rather than contradicting it; no fix warranted | script `measure_correlation_cluster.py`, kept in session scratchpad, not committed (ad hoc measurement, not a repo tool) | 2026-08-17 |
| AF-20260817-28 | 2026-08-17 | measurement | **`FACTOR_CROWDING_THRESHOLD=0.70` — NOT measurable today, and that's the honest finding, not a cop-out.** `quant_scores_history` (the only table holding the 5 `mf_*` factor columns `factor_crowding_multiplier()` reads) has **exactly 3 distinct dates** (2026-08-12→08-14) — a brand-new table, nowhere near the ~30-date minimum `measurement.md` already treats as a floor for anything else on this platform (`screener_breadth`, `earnings_beat_yoy/qoq` were both flagged "not significant, low-power" at 9–27 dates; this is thinner still). Forcing a panel test on 3 dates would be exactly the "dramatic number from a small filtered subsample" trap the panel spec exists to prevent. `quant_scores` itself (the live, non-history table) is a bare current snapshot, PK'd on symbol alone — no date dimension at all. | EVIDENCE | **genuinely open, calendar-constrained** — re-test once `quant_scores_history` has ~30+ distinct dates (at its current cadence, several more weeks); no engineering fix shortens this | — | — |
| AF-20260817-29 | 2026-08-17 | measurement | **`backtest_optimizer.py`'s `UPDATE_THRESHOLD=1.05` — measured, and it is badly inside the noise floor.** Calling the production `Backtester().run()` directly (not reimplemented) with one FIXED param config (`horizon_days=15, min_score=5, max_positions=15, stop_loss_pct=7`) across the only 3 non-overlapping-enough windows the available ~93-day `technical_signals` scored history supports (45-day windows, 12-day step): Sharpe came back **−0.545, −0.171, +1.683** — sign-flipping, a max pairwise swing of 2.229 (**691.8% of the mean**, not 5%). Same class as `recurring-bugs.md`'s already-fixed HMM-regime-retrain finding ("a champion/challenger gate is meaningless if seed noise exceeds the gap"), and here the swing is roughly two orders of magnitude past the 5% margin the gate requires, not merely comparable to it. **Caveat found in the run itself, not glossed over**: window 2 triggered the Backtester's own `win_probability`-discontinuity warning (0.391→0.706 across the window), meaning some of this variance may be a real scoring-logic change mid-window rather than pure noise — but the swing is so large that even a generous discount for that leaves it far past 5%. Separately (pre-existing, not news): every window's own output flagged **27–28% of the traded universe missing OHLCV price history**, a real survivorship caveat on this tool's numbers generally. **Also found live: `app_settings` has never held an `optimal_sharpe` row at all** — the promotion gate has never actually fired a real promotion in this codebase's history, consistent with the pre-existing code comment about the window-depth bug that starved every prior grid-search attempt. | EVIDENCE | **fixed + live-verified 2026-08-17.** Redesigned the promotion gate to mirror `regime_detector.py`'s multi-EM-restart fix for the analogous noise class: the holdout window now splits into up to 5 walk-forward folds (`_split_holdout_folds`, gracefully degrading to 1 fold when history is too short to split, exactly as observed live), and BOTH the challenger and the current live champion config are re-scored on the SAME fold (`_score_folds`) — eliminating the second, separate confound this finding surfaced (the old gate compared a fresh holdout Sharpe against a stale stored number from a different, older run). Promotion now requires a majority of folds-with-enough-trades to favour the challenger (`should_update_multi_fold`), not one point estimate. `_get_current_config` correctly returns `None` in the bootstrap case (no champion ever promoted). 11 new unit tests, one a negative control reproducing the exact measured incident (−0.545/−0.171/+1.683 against a fixed champion) and proving the old single-window logic gave 2 different verdicts depending on which window was picked, while the majority vote gives one stable answer; all 11 fail on import against the pre-fix source. Full pytest 2,059/0 failed, `tsc` clean, `check_recurring_bugs.py` clean. **Live end-to-end smoke test against real production Backtester/DB** (2 grid combos, not the full 300, to stay interactive): correctly detected the bootstrap case, split a real 27-day holdout into exactly 1 fold (below the 2-fold threshold, confirming the graceful-degradation design), ran the real Backtester on it, declined to promote on a negative Sharpe, and left `app_settings` untouched (dry-run). Scratch `backtesting_runs` rows (`opt_%`/`fold_%`) verified cleaned up, 0 remaining | negative-controlled tests in `test_backtest_optimizer.py`, rationale documented in-place in `backtest_optimizer.py`'s own comments naming this row and the HMM analogue | 2026-08-17 |
| AF-20260817-25 | 2026-08-17 | monitoring | `regime_edge_status` (backs `regime-edge-trust-floor`, one of the 3 known stuck-warn checks) is a **6-row overwrite-in-place snapshot last recomputed 2026-08-14**. Found by `/threshold-calibration-audit` (Lane 6, rotation group 4). | INVESTIGATE | **closed, not a bug** 2026-08-17 — traced `ml_calibration.py` (writer of `regime_edge_status`) to `queues.ts:1044`, called inside `processMlDailyOps`, cron `'20 13 * * 1-5'` = 18:50 IST, **weekdays only**. Last weekday before this audit was Friday 08-14 (matches the snapshot exactly); 08-15/16 were Sat/Sun with no scheduled run; today's (08-17) 18:50 IST slot had not yet arrived at audit time (16:36 IST). The staleness is fully explained by the cadence, not a scheduling failure — confirmed via `job_heartbeat` for `ml-daily-ops` and its `event-triggers` substep, both showing the identical last-run timestamp as the Friday snapshot. **A real, separate bug was found while tracing this**, filed as AF-20260817-26 | — | 2026-08-17 |
| AF-20260817-26 | 2026-08-17 | temporal | **`regime-edge-trust-floor`'s own staleness check used raw `daysStale()`, not `tradingDaysStale()` — the exact "Monday reads Friday as 3 days stale" class `recurring-bugs.md` names as a 4-recurrence pattern, in a 5th location the static checker doesn't cover (a hand-rolled 'ML state' check, not `TABLE_FRESHNESS_CHECKS`).** Live-caught while investigating AF-25: `dataQualityChecks.ts`'s `regime-edge-trust-floor` reported `regime_edge_status hasn't refreshed in 3.1d` on Monday 08-17 for a Friday-08-14 snapshot that had not actually missed any scheduled run — the message masked the check's real, more important signal (`HIGH_VOL` regime below the AUC trust floor) behind a false alarm every Monday. | FIX | **fixed + live-verified** 2026-08-17 — swapped `daysStale` → `tradingDaysStale` (drop-in, same signature). Live re-run of `dq:check` immediately after: the check now correctly falls through to `1 of 2 regime(s)…below the 0.55 live-edge trust floor` instead of the stale-refresh message — the real, previously-masked signal now surfaces | **rung 4** — negative-controlled with a deterministic Friday-18:00-UTC → Monday-20:00-UTC scenario (raw gap 3.083 calendar days, clears the old >3 bar; trading-adjusted ~1.08, correctly under it): fails against the unfixed source reproducing the exact live string `"hasn't refreshed in 3.1d"`, passes restored | 2026-08-17 |
| AF-20260815-05 | 2026-08-15 | data | `mf_sector_allocation` empty; `insider_transactions` 75.3d stale; `regime-edge-trust-floor` at 1 of 2 regimes. All three pre-existing DQ warns, none critical. | INVESTIGATE | **closed** 2026-08-18, all three sub-issues independently resolved: `mf_sector_allocation` remains a documented dead-upstream ACCEPT (in-code comment, `mf-sector-allocation-recency`); `insider_transactions` repointed to `insider_trades` by `651ae44` (2026-08-17); `regime-edge-trust-floor` staleness resolved by AF-25/AF-26. Row survived 5 runs before closing — the header's own escalation rule applied | — | 2026-08-18 |
| AF-20260816-16 | 2026-08-16 | docs | `CLAUDE.md:112` said "37 files still **call** `sqlite3.connect`" when 102 call it (37 route through the shim on a default run) — downgraded to a wording fix. | FIX | **closed** 2026-08-18 — superseded by `793ab22`'s Phase 2 close-out; current `CLAUDE.md` no longer makes the "37 files" claim at all (grepped, 0 matches), it now correctly states "6 named files still block Phase 3" | — | 2026-08-18 |
| AF-20260816-15 | 2026-08-16 | stability | `bharat-server` `restart_time` climbed 3→38 with a native libuv abort (`Assertion failed: !(handle->flags & UV_HANDLE_CLOSING)`), twice at 11:51 UTC 08-16. Process-level crash, nothing in app error handling sees it. | ACCEPT | **downgraded from INVESTIGATE to ACCEPT 2026-08-18**, acting on this file's own recommendation from the prior check the same day. Re-confirmed once more: 0 occurrences of the assertion string anywhere after 08-16, across the full log. Rationale for ACCEPT rather than continuing to roll it: (1) root cause was never established and there is no live symptom left to trace — investigating further means guessing at a one-off libuv/Windows async-handle timing issue with no reproduction; (2) `restart_time` climbing since is fully explained by deliberate restarts (deploys, this session's own `pm2 restart` for AF-47) with 0 unexplained crashes; (3) it hasn't recurred in 2+ days despite the server restarting repeatedly since, which is evidence against a systemic trigger. If it recurs, reopen with the new occurrence's timestamp — the ledger's "never delete" rule means this row stays visible for exactly that comparison | — | 2026-08-18 (downgraded, not closed) |

### 2026-08-18 — mid-week full audit-loop run (user-requested, all 8 lanes)

Not a scheduled weekend pass — the user explicitly asked to launch the full loop. Week 34 mod 5 = group 4 (Silent-green), but that group was already run in full yesterday (2026-08-17) with real fixes landed — mechanically re-running the identical group the same week would duplicate yesterday's work rather than close a gap. Deviated deliberately: ran group 3 (Frontend & canonical — `canonical-read-audit`, `shell-parity-audit`, `data-honesty-review`) instead, since 2 of its 3 commands were flagged as genuine coverage gaps in the 2026-08-16 run log and the third (`canonical-read-audit`) hadn't been re-run since.

| ID | Found | Class | Finding | Lane | Status | Immunized | Closed |
|---|---|---|---|---|---|---|---|
| AF-20260818-30 | 2026-08-18 | data-honesty | **Canonical `/alpha` page renders a 99%-confidence SIDEWAYS regime as a bullish green up-arrow.** `CommandCenterDashboard.tsx`'s `REGIME_STYLE` map has no `SIDEWAYS` key and silently falls back to `BULL`'s styling (`REGIME_STYLE[regime?.name ?? 'BULL'] ?? REGIME_STYLE.BULL`). Live-screenshotted 2026-08-18 against the real production row (`market_regimes`: `SIDEWAYS`, `regime_prob=0.9868`, 2026-08-17) — rendered green ▲ "SIDEWAYS REGIME · 99% confidence". 5 sibling components in the same repo (`V6Shell.tsx`, `RiskMetricsDashboard.tsx`, `MarketCommandCenter.tsx`, `HighConvictionPage.tsx`, `HedgeFundResearch.tsx`) already special-case SIDEWAYS with a neutral amber color — confirmed by the same screenshot, where `V6Shell`'s own header chip renders the correct amber badge one component away. Found by `/data-honesty-review`. | FIX | **fixed + live-verified** 2026-08-18 — added a `SIDEWAYS` entry to `REGIME_STYLE`, amber/neutral, matching the 5 sibling components' existing convention exactly. Re-screenshotted the live page: badge now reads neutral amber for the same live SIDEWAYS row | matches existing pattern in 5 sibling components, no new check needed | 2026-08-18 |
| AF-20260818-31 | 2026-08-18 | data-honesty + scoring-authority | **Canonical `/alpha` per-engine score bars render "engine had no data for this stock" identically to "engine scored this stock 0" — and the root cause is one level deeper than it first looked.** `CommandCenterDashboard.tsx:156-160` — `ScoreBar value={pick.ml_score ?? 0}` etc. Live-screenshotted real rows: `OSWALSEEDS` (coverage "2/8") shows ML=0/Technical=0/DL=0 — pixel-identical to a genuine zero score. Found by `/data-honesty-review`. **The frontend fix alone (below) was live-verified as INERT**: a direct query of `unified_recommendations` for OSWALSEEDS/TASTYBITE/GATECH showed `ml_score`/`technical_score`/`dl_score` stored as literal `0.0`, not `NULL` — the frontend's `?? 0` had nothing to key off, it was dead code layered on top of a server-side default. Traced to `unified_ranker.py:2271-2275` (`run()`'s results-dict construction): `screener_stock_score`/`ml_score`/`confluence_score`/`technical_score`/`dl_score` were written unconditionally from `engine_scores[e]` (itself `.get(sym, 0.0)`-defaulted at :2104), while 3 sibling columns 4 lines below (`cs_score`/`breakout_score`/`smart_money_score`) already correctly gate on `'X' in has_data`, per an existing in-place comment explaining exactly why ("otherwise the column silently goes NULL and the collapse becomes invisible"). The 5 were simply never given the same guard. | FIX | **fixed + live-verified + negative-controlled 2026-08-18, both layers.** (1) Frontend: `ScoreBar` renders a greyed "n/a" state for `null`/`undefined`, distinct from a filled zero-width bar. (2) Root cause: added the identical `has_data` guard to the 5 missing columns in `unified_ranker.py`, matching the 3 that already had it — `engine_scores` and `unified` (the actual blend, computed separately at :2110 from `present`) are untouched, so no score/weight/classification changed, only what gets written to the reporting columns (same shape as this file's `_log_recommendations`/`seed_screener_catalog` precedents — not a `verify-gate.mjs`-gated change). Re-screenshotted `OSWALSEEDS` live post-fix: uncovered engines now show "n/a" instead of a bar reading 0 | **rung 4** — new test `test_engine_score_columns_null_not_zero_when_engine_has_no_row` in `test_unified_ranker.py`, negative-controlled (fails against the pre-fix source with `AssertionError`, confirmed via `git stash`; passes restored). Full unified_ranker suite re-run: 203/203 pass | 2026-08-18 |
| AF-20260818-32 | 2026-08-18 | data-honesty | `PortfolioTrackerPage`'s two headline P&L KPI tiles bypass the file's own null-safe `toneClass()` helper (`tone={((totals?.unrealizedPnl ?? 0) >= 0) ? 'up' : 'down'}`), defaulting to bright green whenever `totals` is `undefined` post-query-error (the page's `isLoading` gate doesn't cover an `error` state). Value correctly shows "—" but color reads as a real gain. Narrower trigger than AF-30/31: needs an authenticated session with a failed `getPortfolioInsights` call, not reproduced live this pass. Found by `/data-honesty-review`. | FIX | **fixed** 2026-08-18 — swapped both inline ternaries for the existing `toneClass()` helper (4 call sites, same file already uses it correctly for every per-holding row). Not live-reproduced (requires a query-error state); `tsc --noEmit` + the page's existing render logic confirm the swap is behaviorally inert on the healthy path (same `>=0` semantics) and neutral-grey on the null path | — | 2026-08-18 |
| AF-20260818-33 | 2026-08-18 | data-honesty | `ScreenerBrowserPage`'s reliability columns (`avg_return`, `alpha`) color green/red off `?? 0` while the adjacent text independently renders `"—"` for the same NULL — a null cell can render a green `"—%"`. Separately, no `total_appearances` floor: a screener with 1 appearance gets identical full-saturation styling to one with hundreds (ties to `measurement.md`'s existing "thin-denominator artifact" finding on this same table). Found by `/data-honesty-review`. | FIX (color/null mismatch) · EVIDENCE (sample-size weighting) | **color mismatch fixed** 2026-08-18 — swapped the inline `?? 0` tone ternary for a null-safe helper (grey when null, matching the `"—"` text). **Sample-size floor deliberately NOT added** — that's a display-weighting decision on top of an already-known-thin table, not a pure rendering bug; needs a `measurement.md`-style decision on what floor is defensible, not a mechanical fix | — | 2026-08-18 (color half only) |
| AF-20260818-34 | 2026-08-18 | shell-parity | **v6 (default shell) has no nav path to 7 real, live, working pages** that v1/v2/v3 both link: `agent-auditor`, `agent-optimizer`, `builder`, `export-picks`, `jobs`, `todays-picks`, `early-spotter`. All 7 confirmed live (not stubs) via their tRPC calls existing server-side. `navGroups.ts`'s own header comment documents only 2 deliberate v6 differences, with no mention of dropping these 7 — reads as an unintentional omission at the file's creation (`d79b6ad`), never diffed against siblings since. Found by `/shell-parity-audit`. | FIX | **fixed + verified** 2026-08-18 — added all 7 nav entries to `navGroups.ts`, same shape as existing entries, pointing at the already-live routes. `tsc --noEmit` clean; visually confirmed all 7 now appear in v6's sidebar and each navigates to its working page | — | 2026-08-18 |
| AF-20260818-35 | 2026-08-18 | docs | Stale comment in `V2AppShell.tsx:59` claims `dashboardVersion` defaults to `'v3'` ("THIS shell is what most users actually see") — the default changed to `'v6'` on 2026-08-09 (`fe56250`), 2 days after the comment was written, and was never updated. Found by `/shell-parity-audit`. | FIX | **fixed** 2026-08-18 — comment corrected to name `v6` as the actual default, referencing `App.tsx`'s init state | — | 2026-08-18 |
| AF-20260818-36 | 2026-08-18 | canonical-read | **`HighFlyerWidget` on v6's default landing page shows a measured-negative model's picks with zero caveat.** `high_flyer_candidates`/`flyer_classifier` is documented in `recurring-bugs.md` as AUC 0.81 but IC **−0.041 (t=−9.02)** — "predicts who flies, not when." The adjacent `ModelBacktestStatusCard` on the same page already carries the correct caveat ("never treat this as a trading signal"); this widget doesn't. Found by `/canonical-read-audit`. | FIX | **fixed** 2026-08-18 — added the same disclosure pattern already proven on `ModelBacktestStatusCard` to `HighFlyerWidget`'s candidate list | matches existing proven pattern on the sibling card | 2026-08-18 |
| AF-20260818-37 | 2026-08-18 | canonical-read | **`ScreenerBrowserPage` quotes stale, retracted backtest t-stats** ("t=2.67", "t=2.08", "strongest factor measured here") that `measurement.md`'s 2026-08-12 banner explicitly retracted: both dropped below significance once the exit-pricing bug was fixed, and the file now states "no factor in this harness has a credible positive edge." Neither the component nor the matching router comment was updated after the 2026-08-12 fix. Found by `/canonical-read-audit`. | FIX | **fixed** 2026-08-18 — swapped in the corrected t-stats (1.99/1.10) and removed the "strongest factor"/validated framing, matching `measurement.md`'s current verdict | — | 2026-08-18 |
| AF-20260818-38 | 2026-08-18 | canonical-read | **3 screener-display files show unlabeled bullish/bearish sentiment consensus with no caveat** (`TrendlyneScreenerPanel.tsx`, `ScreenerDetailsModal.tsx`, `PremiumScreenersPage.tsx`) despite `measurement.md`'s finding that screener sentiment labels are keyword-classified off the screener name, never validated, and measured **inverted** (t=−4.61). 3 sibling files reading the same table already carry near-identical disclosure text. Found by `/canonical-read-audit`. | FIX | **fixed** 2026-08-18 — ported the existing disclosure banner text (from `ScreenerIntelligencePage.tsx`/`ScreenerLabPage.tsx`/`SignalIntelligence.tsx`) into all 3 files | reuses an already-proven, already-worded pattern from 3 sibling files | 2026-08-18 |
| AF-20260818-39 | 2026-08-18 | scoring-authority | **`getTechnicalConfluenceSignals` computes an ad-hoc score literally named `unified_score`**, colliding with the canonical `unified_recommendations.unified_score` column — `0.4*(signal_score/10) + 0.4*win_probability + 0.2*(confluence_score/100)`, unrelated to the real one. Consumed by `TodaysPicks.tsx` (reachable from v1/v2/v3's default nav, not v6's). The router's own comment already flags this: "despite the old name, this has never read the unified_signals table." Found by `/canonical-read-audit`. | FIX (rename/disclose) · EVIDENCE (changing the 0.4/0.4/0.2 blend itself) | **field renamed + disclosed 2026-08-18** — renamed the SQL alias and the frontend field from `unified_score` to `confluence_composite_score` (no name collision with the canonical column), and added a `LegacyScoreBanner` to `TodaysPicks.tsx` naming it as a separate ad-hoc blend, not the canonical ranking. **The 0.4/0.4/0.2 weighting itself is untouched** — that's a scoring-formula change and needs `verify-gate.mjs`'s backtest-evidence bar, out of scope for this pass | banner + rename remove the collision permanently; formula itself flagged for a future EVIDENCE pass | 2026-08-18 (rename/disclosure only) |
| AF-20260818-40 | 2026-08-18 | docs | `scoring-authority.md` states `getTopRatedStocks` "reads `unified_recommendations` first, falling back to `stock_scores` only when UR has no rows yet" — true only for `timeframe === 'long_term'`; `intraday`/`swing` go straight to `stock_scores`, never checking UR. Low blast radius: `TopRatedStocks.tsx` already carries a `LegacyScoreBanner` disclosing non-canonical status regardless. Found by `/canonical-read-audit`. | ACCEPT | **closed 2026-08-18 — already fixed by the same-day session that filed it.** Re-checked `scoring-authority.md:19-23` live: it already carries the `long_term`-only qualifier and cites this ID by name. Nothing left to edit; the "open, doc-wording only" status line was stale relative to the file's own content | — | 2026-08-18 |
| AF-20260818-41 | 2026-08-18 | canonical-read | **8 scoring/ranking-shaped tRPC procedures have zero references anywhere** — not UI, not server-internal, not MCP. This read as `scoring-authority.md`'s exact "orphaned procedure = a parallel score nobody wired up" warning shape. | INVESTIGATE | **closed 2026-08-18 — investigated per-procedure rather than force-fixed, and the answer is not what the finding assumed.** One correction and one real conclusion: (1) **`getMLModelRegistry` is NOT orphaned** — the original agent's sweep missed `scripts/ci_smoke_test.ts`, which calls it every CI run (confirmed live: it's in `smoke:ci`'s own 10/10 PASS list from this session's Lane 2). A repo-wide re-grep (not restricted to `src/`) found this the finding's own search scope missed. (2) **The remaining 7 are NOT the dangerous "parallel final score" pattern `scoring-authority.md` warns about at all** — none computes a competing per-stock ranking score; they're read-side analytics (win-rate/Sharpe segments, weight-optimization history, sector breadth, portfolio-correlation, personal trade-journal stats) over tables that are genuinely alive with real data, checked directly: `strategy_performance` 247 rows, `screener_weight_history` 40 rows, `screener_sector_rotation` 477 rows, `unified_signals WHERE signal_source='SCREENER_SURFACING'` 1,243 rows. `signal_actions`/`signal_portfolio_correlation` (the 2 currently-empty ones) both have a live WRITER already wired to a real UI component (`saveSignalAction` ← `AlphaCockpit.tsx`; `correlationService.ts`'s `INSERT INTO signal_portfolio_correlation`) — they're at 0 rows because the write-triggering feature hasn't been used yet, not because nothing writes them. **Every one of the 7 is a built, correct, table-backed feature missing only a UI page that reads it back — a product/frontend decision, not a bug.** Deleting live, correct, real-table-backed code to close an "orphaned" finding would have been actively destructive; wiring up 7 UI pages would have been unscoped feature work. Documenting the investigated, evidenced conclusion IS the correct close for this lane | — | 2026-08-18 |
| AF-20260818-42 | 2026-08-18 | data | **`marketsmojo_financials_history` bloat worsened since last week: 7.3% → 16.3% dead tuples** (4,178,279 live / 813,411 dead). | FIX | **fixed + live-verified 2026-08-18 — real root cause was different from my own first guess, traced not assumed.** Re-checked `n_dead_tup` twice ~40 minutes apart during this session: **static at exactly 813,411 both times** — not actively growing, which ruled out my original hypothesis (the fetch-before-lookup ordering in AF-20260816-20). Checked the table's autovacuum storage params instead: only `autovacuum_analyze_*` had been tuned (migration `1787030000000`); `autovacuum_vacuum_scale_factor` was still the Postgres default **0.2** (20% of table size) — on a 4.18M-row table that's an **~836,000-row trigger**, and 813,411 is almost exactly that, i.e. the table was about to autovacuum on its own, not leaking without bound. The 2026-08-14 write-amplification fix (which correctly cut churn) is the same root cause as `1787030000000`'s ANALYZE fix: less churn means longer to cross a PERCENTAGE threshold, so bloat sits elevated longer on exactly the large tables where it costs most. Manually `VACUUM ANALYZE`d the table live (813,411 → **0** dead tuples, 8.5s, confirmed not a hypertable so no decompression hazard) for the immediate symptom, then added migration `1787080000000` applying the identical flat-threshold treatment (`scale_factor=0, threshold=100000`) `1787030000000` already established as safe for these same 8 large tables, extended to VACUUM. Applied via `npm run migrate:up` against real `POSTGRES_URL`, live-verified the new `reloptions` on all 3 spot-checked tables, `schema:drift` still clean (storage params aren't schema-tracked, consistent with the sibling migration). AF-20260816-20 (fetch-before-lookup HTTP round-trip cost) is separately still real and still open — it's a wasted-network-call efficiency issue, just not the cause of THIS bloat | migration `1787080000000`, same durable mechanism as its `1787030000000` precedent — self-enforcing going forward, not a one-time manual fix | 2026-08-18 |
| AF-20260818-43 | 2026-08-18 | data | **`technical_signals` has 6 frozen columns not previously documented**: `eps_miss_after_streak`, `insider_buy_flag`, `insider_sell_flag`, `wc_deteriorating`, `wc_improving`, `near_expiry_gamma` — identical value across every row on the latest enrichment-complete date (2026-08-13, 2189 rows). Found via a scoped `integrity_sweep.py --table technical_signals` run (see AF-45 for why scoped, not full-repo). Not traced further this pass — could be legitimate (no stock genuinely meets these boolean conditions today) or a writer bug (never actually computed, defaulting to one value). | INVESTIGATE | **closed 2026-08-18, traced per-column — split three ways, none is a writer bug.** (1) **`insider_buy_flag`/`insider_sell_flag`/`near_expiry_gamma` are NOT structurally frozen** — a single-date sample caught them at a coincidental all-zero day; queried across all history they are nonzero 49/45/961 times respectively. False alarm from `integrity_sweep.py`'s one-date sampling, same shape as `avg_engine_track_record` below. (2) **`wc_deteriorating`/`wc_improving` are genuinely 0 in EVERY row, ever** (`SELECT count(*) WHERE col != 0` → 0) — traced to `working_capital_fetcher.py:247-248`, both gated on `ccc_trend is not None`, and `ccc_trend` is exactly the column `measurement.md`'s "7 permanently-NULL columns" section already documents as **arithmetically impossible today**: live-reconfirmed `0 of 1,675 symbols have 2+ fiscal years in `working_capital_history``. Same root cause, same fix timeline (needs a second fiscal year to accumulate) — this is that finding's sibling in boolean-flag form, not a new bug. (3) **`eps_miss_after_streak` is also 0 in every row, ever**, traced to `eps_surprise_fetcher.py:134-141`'s explicit `len(rows) >= 4` gate (needs 4 quarters of surprise history to even evaluate); live-checked `eps_surprise_history` depth — max is 2 rows/symbol platform-wide. Same calendar-constrained class as `measurement.md`'s already-documented `eps_revision_3m_pct`/`target_revision_3m_pct`/`analyst_count_chg` trio (all needing ~76-90d of a table with ~50d of depth) | — | 2026-08-18 |
| AF-20260818-44 | 2026-08-18 | data | `unified_recommendations.avg_engine_track_record` is frozen at one identical value across all 2,188 rows on the canonical ranking table's latest date (2026-08-13). Found via scoped `integrity_sweep.py --table unified_recommendations`. | INVESTIGATE | **closed 2026-08-18, not a bug — it's a single portfolio-wide scalar by design, not a per-symbol feature.** Traced to `unified_ranker.py:1628-1638`'s `_get_avg_track_record()`: `AVG(actual_return_pct) FROM recommendation_log WHERE generated_at >= (today-90d)` — one number, computed once per `run()`, stamped onto every symbol's row that day. Being identical across all rows on a single date is the intended behavior, not a defect. Confirmed it genuinely varies across dates (live query, `GROUP BY computed_at`): 0.25 (08-06) → 0.16 (08-13) → -0.04 (08-18/19) — a real, moving trailing-90d figure, not a hardcoded constant. Same false-positive shape as AF-43's `insider_buy_flag` sub-finding: `integrity_sweep.py`'s single-date "frozen" heuristic cannot distinguish a column that's designed to be a run-wide scalar from one whose writer is stuck. No code change — the name is arguably misleading (reads like a per-engine metric) but that's a naming nitpick, not worth a rename given `avg_track` is read by exactly one write site | — | 2026-08-18 |
| AF-20260818-45 | 2026-08-18 | tooling | **AF-20260816-18's "fixed + verified" streaming claim for `integrity_sweep.py` does not reproduce for a full-repo run** — 0 bytes of output after 20+ minutes. My original hypothesis (`tee`/pipe buffering) was WRONG, traced not assumed. | FIX | **fixed + live-verified 2026-08-18, real root cause traced.** AF-18 fixed *when* each finding prints, correctly — but `sweep_table()` still ran **one query PER COLUMN** (`for col in cols: read_df(...)`), a network round trip for every column of every table. Measured: `technical_signals` alone (303 cols) took **8.07s** for its 303 sequential round trips. Over ~213 tables the accumulated round-trip count, not buffering, is why nothing printed for 20+ minutes — most early tables (alphabetically) have zero findings, so nothing streams, but every one of them still pays its full per-column cost before moving on. Rewrote to ONE query per table via conditional aggregation (`COUNT(col)`/`COUNT(DISTINCT col)` for every column in a single `SELECT`), with a per-column fallback preserved for a table where the batched query itself errors (an exotic/unorderable type must not silently drop every other column's check). `technical_signals`: 8.07s → 3.2s for that table; **full-repo run now completes in 9m05s** (previously: never completed, abandoned at 20+ min both times it was tried). Verified byte-identical output before/after on 6 tables checked in the same instant (`technical_signals`, `unified_recommendations`, `feature_store`, `quant_scores`, `stock_scores`, `marketsmojo_financials_history`) — later drift in `technical_signals`' own dead-column count (58→66 across ~30 min) is live production data changing under a concurrently-running enrichment job, not a regression, confirmed by re-running the scoped check at that later instant and getting 66 both times. 9 minutes for 213 tables is a real, large improvement, not "fast" — flagged honestly, not oversold | **rung 4** — new `test_integrity_sweep.py`, 5 tests pinning `_classify()` (the decision logic both the batched and fallback paths now share) against every outcome: dead, frozen-numeric, not-frozen-text, populated-and-varying, partial-null. Extracting `_classify()` out of the original inline loop is itself the change that makes both paths guaranteed to agree | 2026-08-18 |

| AF-20260818-46 | 2026-08-18 | data-correctness | **`getTechnicalConfluenceSignals` (`/todays-picks`, `TodaysPicks.tsx`) returns exact duplicate rows for the same symbol** — found incidentally while live-verifying AF-39's fix. Live screenshot: 3 pixel-identical `BIL` cards, 3 identical `RISHABH`, 3 identical `RAYMOND` — triggered React's "two children with the same key" console error. | FIX | **fixed + live-verified + negative-controlled 2026-08-18.** Root cause confirmed by direct query, not assumed: `confluence_signals` carries **20 rows/symbol/day** (`confluence-compute`'s 24/7 ~30-min cadence — 4,499 symbols have >1 row on a single day, checked live), so the plain `LEFT JOIN ... cs.computed_at >= ? AND < ?` day-range matched every one of them, not one. Confirmed exactly for the 3 duplicated symbols: 4 rows each under the old join, 1 under the fix. Fixed by replacing the plain `LEFT JOIN` with `LEFT JOIN LATERAL (... ORDER BY computed_at DESC LIMIT 1) cs ON true` — same sargable half-open range (still chunk-exclusion-friendly, still never wraps `computed_at` in `date()`), now takes only the latest row per symbol inside it | **rung 4** — new `technicalConfluenceSignalsNoDuplicates.test.ts`, seeds one `technical_signals` row + 2 same-day `confluence_signals` rows (different `computed_at`/score) against the real router via `createCallerFactory`, asserts exactly 1 output row carrying the LATEST score. Negative-controlled: fails `expected 2 to be 1` against the pre-fix source (`git stash`), passes restored | 2026-08-18 |

**Lane summary this run:** 0 deploy ⚠️ (server behind HEAD again, by construction — restart deferred to end of this remediation pass) · 1 repo ✅ (tsc/check_recurring_bugs/schema:drift/build all clean; vitest 992/2‡/41, pytest 2065/0/230 — ‡ both confirmed self-inflicted DB-contention flakes, pass in isolation) · 2 services ✅ (4/4 online, smoke:ci 10/10) · 3 jobs+DQ ✅ (dq:check 151/155, 0 critical; job_heartbeat 0 jobs at 100% fail; log sweep clean 13 days) · 4 database ⚠️ (0 long txns, but 1 bloated table worsening — AF-42) · 5 frontend ✅ (v6 + v2 both clean, AF-21's fix confirmed holding live) · 6 rotation ✅ (group 3, all 3 commands run via background agent, 12 new findings) · 7 ledger ✅ (this entry, 16 new rows: AF-30…AF-45) · 8 remediate — see below.

**New this run:** AF-30 through AF-46 (17: 12 from Lane 6 + 2 from Lane 3/4 escalations + 2 tooling/data findings + AF-46 found incidentally during Lane 8 live-verification).
**Escalated during Lane 8, not just fixed as scoped:** AF-31 turned out to be a two-layer bug — the frontend null-safety fix was live-verified as inert (the DB already held literal `0.0`, not `NULL`), which forced tracing into `unified_ranker.py` itself. Fixed at both layers, negative-controlled. Read this row before citing AF-31 as "just a display fix."
**Closed this run:** AF-05 (5-run-old, all 3 sub-issues resolved), AF-16 (superseded by later docs work).
**Updated, not closed:** AF-15 (0 recurrence in 2 days, downgraded urgency).

| AF-20260818-47 | 2026-08-18 | data-correctness | **`technical_signals.stop_loss`/`targets` store a ₹-formatted display string, not a number — and two different downstream readers expect a number.** Noticed incidentally in pm2 logs while verifying AF-46/AF-42: `outcome_resolver.py` erroring `invalid input syntax for type double precision: "₹60.33"` on every run since 2026-08-13, plus a live `/todays-picks` screenshot showing `SL ₹NaN`. Traced to `getTradingSetup()` (`technicalSignalsService.ts`, the 2026-08-13 deterministic replacement for the never-ran LLM insight generator): it formats `stopLoss`/`targets` as `"₹1239.39"` for the Telegram digest, and that same formatted string was written straight into `technical_signals.stop_loss`/`targets` — read back as numbers by `outcome_resolver.py`'s `CAST(... AS REAL)` and the frontend's `Number(pick.stop_loss)`. `targets` additionally expected a JSON array by the frontend; a bare `"₹65.00"` string always failed `JSON.parse`, so targets have never rendered since this feature shipped. | FIX | **fixed + live-verified + negative-controlled 2026-08-18.** `unifiedUpsertSql` already regex-extracted a clean number for this exact reason when writing `unified_signals.stop_loss` (`slNumeric`) — extracted that into a shared `parseMoneyToNumber()` helper and applied it to the `technical_signals` write too; `targets` now written as a JSON array of the extracted number. `entryZone` untouched (a genuine formatted range, nothing parses it numerically). **Repaired the 32 existing poisoned rows live** (all from that day's scan): `stop_loss` stripped to clean numeric text, `targets` converted to a JSON array — re-ran `outcome_resolver.py`'s exact `CAST` query after, 0 errors. Frontend hardened too: SL only renders when `Number.isFinite`, degrading silently instead of showing NaN on any future malformed value. Live-verified post-restart: `/todays-picks` shows real `SL`/`T1` values on all 20 picks (previously 0 ever rendered a target, and SL always read NaN) | **rung 4** — 4 new tests on `parseMoneyToNumber` (money string, thousands separator, null/undefined/empty, already-bare number) | 2026-08-18 |
**Still open, escalating:** AF-20 (now AF-42 too, same root cause, bloat direction worsened).

## Run log

Which lanes actually ran, so a partial pass is never mistaken for a complete one.

### 2026-08-15 — week 33, rotation group 3 (Frontend & canonical)

| Lane | Verdict | Notes |
|---|---|---|
| 0 deploy | ⚠️ | 4/4 pm2 online; `bharat-server` 3 restarts, ~4h20m behind HEAD → AF-02 |
| 1 repo | ⚠️ | `tsc --noEmit` clean · pytest 1962 passed/230 skipped · vitest 937 passed/**1 failed** (AF-04) · `check_recurring_bugs.py` clean over 475 py + 136 ts · `npm run build` ✅ 30.4s · `schema:drift` **fail** (AF-03) |
| 2 services | ✅ | :3000 → 200, :8000/:8001/:8002 `/docs` → 200 |
| 3 jobs+DQ | ⚠️ | DQ 144 pass / 3 warn / 0 critical-fail (AF-05) · `integrity_sweep.py` → AF-01 · no job at 100% fail |
| 4 database | ✅ | 1 active / 7 idle connection, nothing >5 min. Earlier in the session two queries ran 70 and 110 min; they cleared on their own — transient, not carried as a finding |
| 5 frontend | **skipped** | Not run this pass — no browser drive performed. Reported as skipped, never as green |
| 6 rotation | **skipped** | Group 3 (`/canonical-read-audit`, `/shell-parity-audit`, `/data-honesty-review`) not run this pass |
| 7 ledger | ✅ | This file created — first baseline, nothing to compare against yet |

**New this week:** AF-01 through AF-08 (first run, all new by definition).
**Fixed and verified:** AF-06, AF-07, AF-08.
**Next run compares against this table.** Lanes 5 and 6 are the gap to close first.

### 2026-08-16 (cont.) — remediation pass

Four fixes, each verified rather than assumed. `tsc --noEmit` clean · `vitest run` 963 passed /
0 failed · `pytest` 2,025 passed / 230 skipped / 0 failed.

| ID | Verification actually run |
|---|---|
| AF-20260815-01 | Ran `integrity_sweep.py --table technical_signals` live. Anchor moved to `2026-08-13` (was the newest date, `2026-08-14`). **Negative-controlled against production**: on 08-14 all six lag-columns read **0 non-null of 2,192 rows** — i.e. the old anchor genuinely did report them DEAD — while on 08-13 they are 1,939 / 2,185 / 1,820 / 1,402 / 1,402 / 1,939. Six confirmed false positives removed. All 7 columns `measurement.md` documents as genuinely never-written are still correctly reported |
| AF-20260816-09 | `tsc` caught a second `usePg` reference the first edit missed — the boot log line. Fixed |
| AF-20260816-10 | Guards removed; `pgHealthy()` path unchanged |
| new | `insightService.getIndexData` fake-index fallback removed (ACTION_ITEMS #14) |

**Correction to this session's own earlier reporting.** ACTION_ITEMS #4 was reported here and in the
register as "confirmed dead code, zero callers". **That was wrong.**
`buildRiskParityWeights` is live — `server.ts:458` calls it through the *default-export namespace*
(`portfolioModule.default.buildRiskParityWeights`), which a named-import grep does not match. Had
the "delete it" option been taken on that evidence, it would have broken the picks-export endpoint's
risk-parity path. The row is corrected in `ACTION_ITEMS.md` with the lesson: grep the bare symbol,
not the import. Same every-reader blind spot `recurring-bugs.md` records for table consumers, in
module form.

### 2026-08-16 — status reconciliation only, NOT an audit run

**No audit lane was executed.** This was a documentation-and-memory reconciliation pass, and it
is recorded here so the next `/weekend-audit` does not mistake three closed rows for evidence
that a sweep ran.

What was verified directly, and only this:

| Row | Check actually run | Result |
|---|---|---|
| AF-02 | `pm2 jlist` uptime vs `git log -1` | 4/4 online, restarted 11:15 UTC > HEAD 07:03 UTC → **closed** |
| AF-03 | `npm run schema:drift` | "Schema clean", 212 file = 212 live → **closed** |
| AF-04 | ownership traced to the landed SQLite-decommission work | **superseded**, re-verify via `vitest run` |

**Still open and untouched this pass: AF-01 and AF-05.** AF-01 (`integrity_sweep.py` anchoring on
raw `MAX(date)`) is now on its second run as open — worth noting because this ledger's own header
says a row surviving 3 runs is itself a finding.

**Lanes 5 and 6 remain the gap** — unchanged since 2026-08-15, still never run.

> **Correction, made during the 2026-08-16 (full sweep) run below:** the two paragraphs above are
> stale. AF-01 *was* fixed and live-verified earlier the same day — the evidence is in the
> "(cont.) — remediation pass" table above this section. Only AF-05 is genuinely open. The two
> 2026-08-16 sections were written out of order and disagree with each other; this note reconciles
> them rather than editing either, per the never-delete rule.

### 2026-08-16 (full sweep) — first run to execute Lane 6

First pass in this ledger's history to run the deep-audit rotation. Working tree was dirty
(69 changed files, 51 staged — a concurrent session's SQLite-decommission work), so suite results
below are "green including that work", not "green at HEAD".

| Lane | Verdict | Notes |
|---|---|---|
| 0 deploy | ⚠️ | 4/4 pm2 online, but server behind HEAD again (AF-14) and `restart_time` 3→38 with a native libuv abort (AF-15) |
| 1 repo | ✅ | `tsc --noEmit` clean · `vitest run` **963 passed / 0 failed** (102 files) · `pytest` **2,025 passed / 230 skipped / 0 failed** · `check_recurring_bugs.py` clean over 480 py + 136 ts · `schema:drift` clean (212=212) · `npm run build` ✅ |
| 2 services | ✅ | :3000 → 200; :8000/:8001/:8002 `/docs` → 200; `smoke:ci` **10/10 procedures** |
| 3 jobs+DQ | ⚠️ | DQ **149/154, 0 critical**. 5 warns → AF-05, AF-17. `job_heartbeat`: **0 jobs that never succeed**; 1 job currently failed (AF-12). `integrity_sweep` unusable at full scope (AF-18) |
| 4 database | ✅ | 1 active / 6 idle; **0 queries >5min**. Two `idle in transaction` (869s, 405s) observed mid-run and cleared on their own — transient, same as last week. 1 bloated table (AF-20). **81 tables still carry zero planner statistics** (was 300/419 on 2026-08-15 — improving, not closed) |
| 5 frontend | ⚠️ **partial** | Server renders; `App.tsx` default is `v6`, confirmed at :221. **No browser drive, no console-error check, no screenshot** — reported as partial, not green. Also: pm2 serves the **Vite dev middleware**, not the production build |
| 6 rotation | ✅ **first ever** | Ran across all 5 groups rather than week-33's group 3 alone: data-coverage, cross-writer-collision, temporal-correctness, test-integrity, threshold-calibration, canonical-read, job-runtime, measurement-integrity |
| 7 ledger | ✅ | This entry; 10 new rows |
| 8 remediate | **not run** | Findings collected and triaged into lanes only. `audit-loop` not invoked — no fixes applied this pass |

**New this week:** AF-11 … AF-20 (10).
**Unchanged from last week:** AF-05 (now on its 3rd run — escalate per the header rule).
**Fixed and live-verified:** none this pass (Lane 8 not run).

**Three claims tested and DISPROVED rather than reported** — recorded because a clean audit that
only lists hits is not showing its work:
1. *16 SQLite `date('now','-N days')` calls in `dataQualityChecks.ts` are Postgres-invalid.* They
   fail on a raw psycopg2 probe, but `sqlTranslate.ts:118-119` rewrites both forms before
   execution and documents the choice. **Not a bug.**
2. *`technical-signals-feature-coverage` was narrowed to a 5-column allowlist, reopening the blind
   spot it was built to close.* The generic `jsonb_each` implementation is intact at :1831; the
   5-column list is a **separate** check. **Not a bug** — but reading it produced AF-11, which is.
3. *`marketsmojo_financials_fetcher`'s 40-min kill starves the tail of the alphabet* (the
   `trendlyne_adv_tech` no-resumability shape). Coverage is A:149…Z:14 across 1,680 symbols.
   **Disproved by the data**; downgraded to the slow-cycle finding AF-20.

### 2026-08-16 (cont. 2) — Lane 8 remediation

Triage: **3 FIX** (closed) · **1 EVIDENCE** (AF-11, reclassified *out of* FIX during the work) ·
**2 INVESTIGATE** (AF-12, AF-15 — owner decision) · **1 ACCEPT** (AF-19) · **1 RETRACTED** (AF-16) ·
**3 carried** (AF-05, AF-14, AF-20).

| Gate | Result |
|---|---|
| `tsc --noEmit` | clean |
| `vitest run` | **965 passed / 0 failed** (963 before + 2 new negative-controlled tests) |
| `pytest` (full, incl. chatbot) | **2,025 passed / 230 skipped / 0 failed** |
| `check_recurring_bugs.py` | clean, 480 py + 135 ts |
| `schema:drift` | clean, 212 = 212 |
| `dq:check` | **148/155, 0 critical** — count moved 154→155 because AF-13's new check was added, and it correctly FAILS |

**Two findings were destroyed by their own remediation, and that is the useful part of this pass:**

- **AF-11 was not mechanically safe.** The proposed fix (adopt the heartbeat anchor its two
  siblings use) was measured before being applied and makes the check *worse*: dead-column counts
  run 59/60/45/46/**15** across 08-10…08-14, and the two candidate anchors disagree by 31 columns
  on the same table on the same day. Different enrichment writers land on different dates, so no
  anchor is correct. Moved to EVIDENCE, unfixed, with the per-date table recorded.
- **AF-16 was simply wrong and is retracted.** I compared a static `grep` (102 files) against
  `conftest.py`'s runtime `_SHIM_USERS` counter (37) and called it a 2.8× understatement. They
  measure different things; the ~65-file gap is `test_live_datasource_*.py` bodies skipped by
  default. Only a wording inaccuracy in `CLAUDE.md:112` survives.

**Not committed.** The working tree already carries 69 files from a concurrent session's
SQLite-decommission work, and `CLAUDE.md` requires committing by explicit path after re-checking
`git status`. The four files this pass touched are `src/server/dataQualityChecks.ts`,
`src/server/__tests__/dataQualityChecks.test.ts`, `scripts/integrity_sweep.py`, and this ledger.

**Rotation coverage gap that remains:** `/fetcher-accuracy-review`, `/migration-safety-review`,
`/ml-promotion-gate-review`, `/shell-parity-audit`, `/data-honesty-review`, `/trpc-surface-review`
and `/signal-accuracy-review` were exercised only through their static/live sweep components, not
end-to-end. Lane 5's browser drive is still the single largest never-executed check in this file.

### 2026-08-17 — week 34, rotation group 4 (Silent-green), first pass with a real Lane 5 drive

First run where Lane 5 was an actual browser drive with screenshots (prior runs reported it
skipped or partial). It is what found this run's headline bug — a code-only review of
`V2Dashboard.tsx` would not have surfaced a 100× display-scaling error; only looking at the
rendered number against its known source value did.

| Lane | Verdict | Notes |
|---|---|---|
| 0 deploy | ⚠️ | 4/4 pm2 online. `bharat-server` up_since 03:22 UTC, `HEAD` at audit start was `9ce7138` (11:46 IST) — **1 commit ahead, uncommitted-this-pass work aside**. `restart_time` 38→40 (2 more since last week), but both new restarts trace to a deploy restart at 08:52 IST matching commit `19b82b2`, not a crash — no libuv abort recurrence found in today's log window (last one was 08-16, AF-15, still open/untouched this pass) |
| 1 repo | ✅ | `tsc --noEmit` clean · `vitest run` **988 passed / 41 skipped, 0 failed** (105 files) · `pytest` **2,048 passed / 230 skipped / 0 failed** · `check_recurring_bugs.py` clean, 484 py + 135 ts · `schema:drift` clean (212=212) · `npm run build` ✅. All four re-run after this pass's fixes landed, not just once at the start |
| 2 services | ✅ | :3000 → 200 (one transient curl blip, retried clean); :8000/:8001/:8002 `/docs` → 200 |
| 3 jobs+DQ | ⚠️ | DQ **149/155, 0 critical**. Same 6 non-pass rows as last week (AF-05's three uninformative-warn checks, AF-13's trendlyne coverage FAIL, `nse-filed-corporate-actions-freshness` warn). `job_heartbeat`: **0 jobs that never succeed, 0 currently failed** (AF-12 from last week is gone — resolved or aged out). `trendlyne-midweek` had its first success since 08-04 today → AF-12 updated, not closed |
| 4 database | ✅ | 1 active / 8 idle / 6 null-state, **0 queries >5min**, 0 long transactions. 1 bloated table, same as last week (`marketsmojo_financials_history`, 7.3% dead — improved from 9.3%, autovacuum ran twice today). Compression: `macro_asset_prices` still 0/6 compressed chunks — not investigated this pass, small table |
| 5 frontend | ✅ **first real drive this ledger** | v6 (default shell): loads clean, real numbers, no console errors, "as of 2026-08-14" honestly dated rather than silently presented as live. v2 spot-check: **found AF-21**, the alpha_vs_nifty ×100 bug, fixed and re-screenshotted to confirm |
| 6 rotation | ✅ | Week 34 % 5 = group 4 (Silent-green): `/temporal-correctness-audit`, `/test-integrity-audit`, `/threshold-calibration-audit` — all three run via background agent, first attempt hit the session's own API rate limit and failed cleanly (reported as failed, not faked), retried successfully after the 4:10pm reset |
| 7 ledger | ✅ | This entry; 5 new rows (AF-21…AF-25) |
| 8 remediate | ✅ **partial, same-session** | 3 of 5 new findings fixed inline rather than deferred to a separate pass: AF-21 (display bug) and AF-22 (2 temporal write-anchor bugs, same pattern as 11 prior fixes) — both mechanically safe, no scoring/threshold touched, so no `verify-gate.mjs` backtest requirement applies. AF-23/24/25 correctly left as EVIDENCE/INVESTIGATE — they'd need a measurement or a job-schedule trace this pass didn't do |

**New this week:** AF-21 through AF-25 (5).
**Fixed and live-verified this pass:** AF-21 (screenshot before/after), AF-22 (tests + full suite re-run).
**Unchanged from last week:** AF-11, AF-14 (recurs every commit by construction, not re-checked this pass), AF-15, AF-16, AF-18 wording, AF-19, AF-20.
**Updated, not closed:** AF-12 (first success in 12 days, insufficient to call the 405 resolved).
**Still open, 3rd+ run:** AF-05 (now run 4) — per this file's own header, an open row surviving 3 runs is itself a finding; escalate to a real ACCEPT-with-rationale or ownered fix next pass rather than rolling it a 5th time.

**Temporal audit's own coverage statement** (from the agent's report, preserved here rather than
re-summarized): task 1 (write anchors) was grepped across the ~55 files touched by the last 7
days of commits, not the full 180-hit universe — not a repo-wide claim. Tasks 2, 4, 5, 6 were
exhaustive within their scoped file lists. No new temporal signature beyond the existing 6 was
found.

**Test-integrity audit found zero new findings** — everything sampled (17 files, prioritized by
risk: mirror-consistency suites, the one new test file this week, monkeypatch/dotenv/split-engine
grep hits) was either already clean or a previously-documented bug already fixed by an earlier
session. Read as a genuine clean bill for the sampled slice, not proof the other ~340 test files
are clean — same "spot-check, not exhaustive" caveat this file's own header expects to be stated.

### 2026-08-18 (cont. 3) — reconciliation pass, no new sweep

User asked to review this ledger + `recurring-bugs.md` for pending items, verify each against live
data before touching it, and fix confirmed-still-open ones. **Not a new Lane 6 rotation** — this
pass only reconciled existing open rows (per this skill's own step 5: "reconcile at the start of
every run: re-check each open row before hunting new ones"). No new findings were manufactured to
pad the count.

| ID | Action this pass |
|---|---|
| AF-20260818-47 | **Re-verified live, not re-fixed.** pm2-err.log showed 3 `"₹60.33"` CAST errors that read as post-fix at first glance; traced the timestamp format (pm2 log uses local IST, not UTC — easy to misread) and confirmed all 3 predate the fix's 16:10 IST restart. Live query: 0 rows anywhere in `technical_signals` would fail the CAST today. No regression |
| AF-20260818-40 | **Closed** — rule-file wording was already corrected by the same-day session that filed it; the "open, doc-wording only" status was stale relative to the file's own content, not a real gap |
| AF-20260818-43 | **Closed, traced per-column.** 3 of 6 columns (`insider_buy_flag`/`insider_sell_flag`/`near_expiry_gamma`) were a single-date sampling artifact, not actually frozen (nonzero 49/45/961 times across full history). The other 3 (`wc_deteriorating`/`wc_improving`/`eps_miss_after_streak`) are genuinely 0 in every row ever written, but traced to the SAME already-documented calendar constraints `measurement.md` records for `ccc_trend` and the analyst-revision trio — not a new writer bug |
| AF-20260818-44 | **Closed, traced.** `avg_engine_track_record` is a single portfolio-wide 90-day trailing scalar by design (`unified_ranker.py`'s `_get_avg_track_record()`), correctly identical across every row on one date and confirmed to genuinely vary across dates (0.25→0.16→-0.04, 08-06 to 08-19). Another instance of `integrity_sweep.py`'s single-date sample being unable to distinguish a by-design scalar from a stuck writer |
| AF-20260816-20 | **Investigated further, left open with a corrected diagnosis.** The finding's own suggested fix ("move the lookup before the fetch") is infeasible as stated — you cannot know a cell changed without the response. A real fix needs a new per-symbol "last checked" tracking mechanism (a migration), which is out of this pass's mechanical-fix scope; documented so the next session doesn't re-propose the same infeasible reorder |
| AF-20260816-12 | **Updated, still open.** Job heartbeat unchanged since 08-17T03:56 UTC; checked the actual cron (`30 14 * * 2`, Tue 14:30 UTC) against today's date and confirmed this week's regular slot simply hasn't fired yet as of this check — not a stall |
| AF-20260816-15 | **Updated, still open but colder.** Re-grepped the full 436,903-line error log (not a tail): still 0 recurrences of the libuv assertion since 08-16, now spanning 2 full days. Recommending a future run downgrade this to ACCEPT rather than rolling it again |
| AF-20260816-14 | **Re-verified clean this pass**, by construction still an open recurring check (not closeable). Confirmed the file-edit→restart→commit ordering that makes "commit timestamp after restart timestamp" not actually mean "server is stale" — traced explicitly rather than assumed, since a naive read of the two timestamps looks like a fresh instance of this exact bug class |

**No code changes landed this pass** (`git status --short` clean before and after) — every
investigated item either turned out to already be fixed by a prior session, or resolved to "not a
bug" once traced against live data, which is itself the point of the "verify before fixing"
instruction this pass was run under. `tsc --noEmit` reconfirmed clean; no `.ts`/`.py`/migration
touched, so `vitest`/`pytest`/`schema:drift` were not re-run (no code path they cover changed).

**Still open, unresolved this pass (correctly, per their own lane):** AF-11 (EVIDENCE — anchor
choice has no correct answer, needs a redesign not a value swap), AF-16-successor wording (closed
elsewhere already), AF-19 (ACCEPT, latent), AF-20 (INVESTIGATE, real fix needs a migration), AF-28
(EVIDENCE, calendar-constrained — needs ~30 dates in `quant_scores_history`), AF-33/AF-39 partial
remainders (EVIDENCE — sample-size floor / formula reweight, both need a measurement pass, not a
mechanical fix).

### 2026-08-18 (cont. 4) — follow-up fix pass on the "correctly not fixed" items

User asked to actually resolve the 4 items the previous pass left as "investigated, still open
with better diagnosis." Three of the four are monitoring/recheck items with no code to write
(re-verifying a live signal isn't a code change); one (AF-20) had a genuine buildable fix once the
earlier pass diagnosed what was actually needed.

| ID | Action taken |
|---|---|
| AF-20260816-20 | **Built the fix.** New table `marketsmojo_financials_checked` (migration `1787090000000`) tracks per-symbol "last checked" independent of `fetched_at`; `run()` skips an already-checked symbol within a 7-day window on the default full-universe sweep, bypassed by `--full`/explicit `--symbols`. Also surfaced: this job is scheduled **weekly** (`processMlWeeklyRetrain`), not the ~2-day cadence the original finding assumed — doesn't change the fix, changes how much it saves. Live end-to-end verified (fetch → `mark_checked` writes a real row → re-run correctly skips with 0 HTTP calls), 4 new tests negative-controlled, migration applied to real `POSTGRES_URL`, `schema:regen`+`schema:drift` clean. **Closed, FIX lane** |
| AF-20260816-12 | Re-checked `job_heartbeat` — unchanged since the last pass (03:56 UTC 08-17). Confirmed via the actual cron pattern that this week's regular Tuesday 14:30 UTC slot had not yet fired at check time (11:17 UTC, ~3h early). Nothing to fix — the job is not stalled, it's not due. **Left open**, correctly; will resolve itself once today's scheduled run completes and can be checked |
| AF-20260816-15 | Re-confirmed 0 recurrences (full-log grep, not tail). **Downgraded INVESTIGATE → ACCEPT** with rationale recorded in the row: no live symptom left to trace, restart-count growth fully explained by deliberate restarts, 2+ clean days despite repeated restarts since. This IS the resolution for a cold, unreproducible crash — continuing to carry it as INVESTIGATE with no new lead would just roll it a 4th+ time for no gain |
| AF-20260816-14 | Re-verified server is in sync with `HEAD` (no `.ts` changed this pass, so no restart needed). This row is a recurring check by construction, not a bug — "resolving" it means confirming current state each pass, which is what happened |

**Check stack, full, after AF-20's changes**: `tsc --noEmit` clean · `vitest run` **999 passed / 41
skipped, 0 failed** · `pytest` **2,077 passed / 230 skipped, 0 failed** (2,073 before + 4 new) ·
`check_recurring_bugs.py` clean, 488 py + 135 ts · `schema:drift` clean, 213=213 (regenerated
after the migration, not stale).

**Only 1 of the 4 items had actual code to write.** The other 3 not landing a code change this
pass is the correct outcome, not an incomplete one — AF-12 has no action until its cron fires,
AF-15 has no lead to chase, and AF-14 is a standing check that stays open by design.
