pandas
yfinance
ta
sqlalchemy
numpy
requests
nse
beautifulsoup4
frozendict
curl_cffi
# Fetcher-only (no scraping/spiders): curl_cffi Chrome-TLS-impersonated HTTP client used by
# tl_fetch.py for trendlyne.com's AWS WAF. After installing/upgrading, browsers are NOT
# required — only `Fetcher` is imported, so `scrapling install` is unnecessary here.
scrapling[fetchers]
rich
psycopg2-binary
ipython
markdown-it-py
mdurl
transformers
# Install CUDA build (RTX 5050 / CUDA 13.x uses cu128 wheels):
# python -m pip install "torch==2.9.1+cu128" torchvision torchaudio --index-url https://download.pytorch.org/whl/cu128
torch>=2.9.1
torchvision>=0.17.0
pytorch-forecasting>=1.0.0
pytorch-lightning>=2.2.0
hmmlearn>=0.3.0
shap>=0.45.0
scikit-learn>=1.4.0
httpx
langchain>=0.3.0
langgraph>=0.2.0
langchain-community>=0.3.0
langchain-google-genai>=2.0.0
chromadb>=0.5.0
sentence-transformers>=3.0.0
duckduckgo-search>=6.0.0
pytest>=8.0.0
pytest-asyncio>=0.23.0
