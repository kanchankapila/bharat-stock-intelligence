# New, isolated infrastructure -- deliberately different ports from the live
# system (Postgres 5433, Redis 6379) so both stacks can run side by side.
DATABASE_URL=postgresql://greenfield:greenfield@127.0.0.1:5434/greenfield
REDIS_URL=redis://:greenfield@127.0.0.1:6380
S3_ENDPOINT=http://127.0.0.1:9000
S3_ACCESS_KEY=greenfield
S3_SECRET_KEY=greenfield123
S3_BUCKET=greenfield-raw
