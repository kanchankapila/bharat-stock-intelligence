// Stage 0 scaffold only. Typed procedures (tRPC), auth, serving-projection
// reads land in Phase 1 (GREENFIELD_STOCK_ANALYSIS_ARCHITECTURE.md). This app
// may import contracts/db/observability -- never ingestion or provider-sdk
// (enforced by eslint.config.js boundaries/element-types).
export const PACKAGE_NAME = '@greenfield/api';
